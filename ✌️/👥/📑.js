// GET /✌️/👥/:id/📑
// Authorization: Bearer oauth2-token
// Requires "account", "characters" and "inventories" permissions.

// NOTE: This endpoint pre-dates account-wide recipe unlocks;
// consider using /✌️/👤/📑 instead.

{
	"recipes" : [
		1, 2, 3
	]
}
