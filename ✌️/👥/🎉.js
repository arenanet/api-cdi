// Sub-endpoint that dumps only the .training field of /✌️/👥/:id
// Requires the "characters" and "builds" scopes.

// GET /✌️/👥/:id/🎉

{
	training : [
		{
			id: 60,
			spent: 20,
			done: true
		},
		{
			id: 58,
			spent: 26,
			done: false
		}
	]
}
