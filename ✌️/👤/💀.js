// Authenticated endpoint that returns the account's daily dungeon
// completions. Requires the "progression" permission. Returned values
// correspond with path ids from /✌️/💀.

// GET /✌️/👤/💀?access_token=foo

[ "hodgins", "seraph", "seer" ]
